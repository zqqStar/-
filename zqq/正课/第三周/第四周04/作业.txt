1.先自逆字符串判断在打印出来排列过的内容 例如有一个字符数组的内容为:“student a am i”,
请你将数组的内容改为"i am a student".

2.封装一个判断车牌号是否限行的函数
限行的规则是周一限行1和6
周二限行2和7
周三限行3和8
周四限行4和9
周五限行5和0
 var now =new Date();
    var weekday =now.getDay();
    console.log(weekday);
这三句代码是获取今天是星期几 周日是0
例如车牌豫A56B0X从末尾开始算第一个阿拉伯数字作为参照

3.求bmi，
要求有一个求BMI的方法，该方法是带参数的函数,如果bmi值为正常范围区间就bmi返回出来，根据返回的bmi计算出这个人
的bmi值为多少，叫xx名字，有多高和多重，体型是个啥